/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/Book-Template",
    output: "export",
    reactStrictMode: true,
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
